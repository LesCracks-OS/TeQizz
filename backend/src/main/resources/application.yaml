spring:
  application:
    name: teqizz

  profiles:
    default: dev

  # Flyway owns schema *changes* going forward. baseline-on-migrate lets it adopt the
  # already-existing schema (created previously by ddl-auto) as version 0, then apply
  # V1+ migrations from db/migration. New entity changes MUST ship a matching migration.
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            scope: read:user, user:email
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: profile, email

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}
  storage:
    minio:
      url: ${MINIO_URL:http://localhost:9000}
      public-url: ${MINIO_PUBLIC_URL:${MINIO_URL:http://localhost:9000}}
      access-key: ${MINIO_ACCESS_KEY:minioadmin}
      secret-key: ${MINIO_SECRET_KEY:minioadmin}
      bucket: ${MINIO_BUCKET:teqizz-avatars}
